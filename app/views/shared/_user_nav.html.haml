.user-nav.col-3-4
  %a{href: revised_circles_url}
    %i.fa.fa-check-square-o
    Ver círculos revisados
  %a{href: circles_url}
    %i.fa.fa-child
    Ver círculos nuevos
  %a.right{href: search_circles_url}
    %i.fa.fa-search
    Buscar en Facebook
.user-loggin.right
  - if current_user
    Signed in as
    = succeed "!" do
      %strong= current_user.name
    = link_to "Sign out", signout_path, id: "sign_out"
  - else
    = link_to "Sign in with Facebook", "/auth/facebook", id: "sign_in"